#include "docker.h"

Docker::Docker() {

}

void Docker::loop(Context *context, Event *event) {
    UNUSED(context);
    UNUSED(event);
}

void Docker::render(Renderer *renderer) {
    UNUSED(renderer);
}
